load("//cuttlefish/bazel:rules.bzl", "cf_build_test", "cf_cc_binary")

package(
    default_visibility = ["//:android_cuttlefish"],
)

cf_build_test(name = "refresh_groups_build_test")

cf_cc_binary(
    name = "cvd_refresh_groups",
    srcs = ["cvd_refresh_groups.cc"],
)
