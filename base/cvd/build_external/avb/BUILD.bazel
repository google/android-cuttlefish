load("//cuttlefish/bazel:rules.bzl", "cf_build_test")

package(default_visibility = ["//:android_cuttlefish"])

cf_build_test(
    name = "cf_build_test",
    srcs = [
        "BUILD.avb.bazel",
        "avb.MODULE.bazel",
    ],
)
