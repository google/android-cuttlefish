syntax = "proto2";

message Duration {
  required int64 seconds = 1;
  required int32 nanos = 2;
}

message Timestamp {
  required int64 seconds = 1;
  required int32 nanos = 2;
}

enum UserType {
  GOOGLE = 0;
  EXTERNAL = 1;
}
