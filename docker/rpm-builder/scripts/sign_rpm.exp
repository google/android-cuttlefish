#!/usr/bin/expect

# Sign RPMs by sending the passphrase.
set filename [lindex $argv 0]
set passphrase [lindex $argv 1]
spawn rpm --addsign $filename
expect -exact "Enter pass phrase: "
send -- "$passphrase\r"
expect eof
