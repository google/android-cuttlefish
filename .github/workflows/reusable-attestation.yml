name: Reusable Container Attestation Job

on:
  workflow_call:
    secrets:
      token:
        required: true
    inputs:
      registry:
        default: 'ghcr.io'
        required: false
        type: string
      username:
        default: ${{ GITHUB.ACTOR }}
        required: false
        type: string
      checksums:
        required: true
        type: string
      digest:
        required: true
        type: string
    outputs:
      metadata:
        value: ${{ jobs.attestation.outputs }}
        description: "Attestation output"

jobs:
  attestation-matrix:
    runs-on: ubuntu-latest
    outputs:
      attestation: ${{ steps.metadata.outputs.metadata_rpm_builder }}
    strategy:
      max-parallel: 4
      matrix:
        target: [ rpm-builder, rpm-testing, rhel-integration, rhel-orchestration ]
    steps:

      - uses: 'docker/login-action@v3'
        id: container-registry
        with:
          registry: ${{ INPUTS.REGISTRY }}
          username: ${{ INPUTS.USERNAME }}
          password: ${{ SECRETS.TOKEN }}

      - uses: 'actions/attest-build-provenance@v2'
        id: attestation
        with:
          subject-name: ${{ INPUTS.REGISTRY }}/${{ INPUTS.USERNAME }}/${{ MATRIX.TARGET }}:latest
          subject-checksums: ${{ INPUTS.CHECKSUMS }}
          subject-digest: ${{ INPUTS.DIGEST }}
          push-to-registry: true

      - id: attestation-step-output
        run: |
          OUTPUTS='${{ toJSON(steps.metadata.outputs) }}'
          TARGET=`echo metadata-${{ MATRIX.TARGET }}} | tr - _` 
          echo ${TARGET}=${OUTPUTS} >> $GITHUB_OUTPUT

          echo TARGET: ${TARGET} >> $GITHUB_STEP_SUMMARY
          echo METADATA: ${OUTPUTS} >> $GITHUB_STEP_SUMMARY

  attestation-matrix-output:
    runs-on: ubuntu-latest
    needs: attestation-matrix
    steps:
      - run: |
          echo attestation='${{ toJSON(needs.attestation-matrix.outputs) }}' >> $GITHUB_OUTPUT
          echo attestation='${{ toJSON(needs.attestation-matrix.outputs) }}' >> $GITHUB_STEP_SUMMARY
