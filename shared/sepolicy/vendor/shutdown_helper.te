type shutdown_helper, domain;
type shutdown_helper_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(shutdown_helper)

# set sys.powerctl
set_prop(shutdown_helper, powerctl_prop)

type shutdown_helper_device, file_type;

allow shutdown_helper device:dir r_dir_perms;
allow shutdown_helper shutdown_helper_device:chr_file rw_file_perms;
